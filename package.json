{
	"name": "local-copilot",
	"version": "0.1.0",
	"description": "Local-LLM text processing for Obsidian via Ollama: summarize, rewrite, proofread, custom prompt commands, and a chat sidebar. 100% local, no API keys.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"ollama",
		"llm",
		"local-llm"
	],
	"author": "timofeevio",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"builtin-modules": "3.3.0",
		"esbuild": "^0.25.0",
		"obsidian": "latest",
		"tslib": "2.8.1",
		"typescript": "^5.7.3"
	}
}
